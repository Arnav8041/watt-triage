// How long DecisionRow's expand/collapse animation takes. ToolTrace waits past this before
// measuring scroll position, so the two must stay in sync — hence the shared constant.
export const EXPAND_TRANSITION_SECONDS = 0.25;
